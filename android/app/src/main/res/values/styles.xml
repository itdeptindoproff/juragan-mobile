<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        windowBackground DIPAKSA putih. Parent-nya DayNight, jadi tanpa baris ini
        window background ikut mode gelap sistem dan berubah abu gelap — terlihat
        sebagai pita berwarna beda di area taskbar, karena bagian layar yang tidak
        dicat WebView memakai warna ini. Tampilan web-nya selalu terang, jadi
        latarnya harus selalu putih agar menyatu dengan BottomNav.
        Ubah bersamaan dengan warna BottomNav bila desainnya berganti.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>